import { describe, it, expect } from 'bun:test';

describe('example', () => {
    it('is an example', () => {
        // This is an example test. Replace it with your own tests.
        expect(1 + 1).toBe(2);
    });

    it('');
});
